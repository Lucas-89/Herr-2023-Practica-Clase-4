@model List<Movie>
    
<div class="text-center">
    <h1 class="display-4">Lista de Peliculas</h1>
   
</div>
@*
<ul>
    @foreach (var item in Model)
    {
       <li><a asp-action="Detail" asp-route-code="@item.Code">@item.Name</a></li> 
       <li>@item.Review</li>
       <!--aca dice item.code porque usa el Get de Service-->
    }
</ul>

<a asp-action="Create">Nueva Peli</a>  <!-- esto va para el servicio create-->

<br>
<br>
*@
<table class="table table-striped">
  <thead>
    <tr>
      <th scope="code">Codigo</th>
      <th scope="col">Nombre</th>
      <th scope="col">Duracion (Min)</th>
      <th scope="col">Categoria</th>
      <th scope="col">Review</th>
    </tr>
  </thead>
  <tbody>
    @foreach (var item in Model)
    {
      <tr>
      <td>
        <a asp-action="Detail" asp-route-code="@item.Code"> @item.Code</a>       
        </td>
      <td>@item.Name</td>
      <td>@item.Minutes</td>
      <td>@item.Category</td>
      <td>@item.Review</td>
    </tr>
    }
  </tbody>  
    
</table>

<div>
<a asp-action="Create" button class="btn btn-success">Nueva Pelicula</a>
</div>